services:
  pihole-toggle:
    build: .
    container_name: pihole-toggle
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PIHOLE_URL=${PIHOLE_URL}
      - PIHOLE_PASSWORD=${PIHOLE_PASSWORD}
